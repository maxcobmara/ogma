- model_class = TravelClaimMileageRate
.page-header
  %h1= t 'staff.travel_claim_mileage_rates.edit'
  
.row
  = form_for [:staff, @travel_claim_mileage_rate],  html: { multipart: true, class: 'form-horizontal' }  do |f| 
    = render 'shared/error_messages', object: f.object
    .col-md-9.col-md-offset-2
      %br
      = render 'form', :f => f
               
      .col-md-10.col-md-offset-1
        .form-group
          .col-sm-offset-2.col-sm-2
            = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), staff_travel_claim_mileage_rates_path, :class => 'btn btn-default'
          .col-sm-4
            = f.submit t('update'), class: "btn btn-primary" 
 
