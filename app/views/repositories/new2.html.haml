- model_class = Repository
.page-header
  %h1= t 'repositories.new2'
  
= render 'shared/flashes'

.row.fluid
  = form_for @repository, html: { multipart: true, class: 'form-horizontal' }  do |f| 
    = render 'shared/error_messages', object: f.object
    = f.hidden_field :remark, value: 'digital'
    = render :partial => 'repositories/digital_library/form', :locals => {:f => f}
    
    .col-md-offset-1
      .form-group
        .col-sm-offset-4.col-sm-1
          = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), index2_repositories_path, :class => 'btn btn-default'
        .col-sm-offset-0.col-sm-2
          = f.submit t('create'), class: "btn btn-primary"
 
