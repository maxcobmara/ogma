.col-md-10.col-md-offset-1
  %dl.dl-horizontal
  
    %dt=t 'asset.loan.is_returned'
    %dd
      =h @asset_loan.returned_on.try(:strftime, "%d %b %Y")
      =t('by2')+@asset_loan.receivedofficer.try(:name) if @asset_loan.receivedofficer
  
    %dt= t 'asset.loan.remarks'
    %dd=h @asset_loan.remarks 

