- if bed.tenants.last.try(:keyreturned) != nil
  Empty
- else 
  - if bed.tenants.last.try(:force_vacate)==true
    Empty
  - else
    = bed.tenants.last.try(:student).try(:name) || "Empty"
  /= bed.tenants.last.try(:student).try(:name) || "Empty"
%br
= bed.try(:parent).try(:combo_code)
%br
= bed.name