- model_class = StudentCounselingSession
.page-header
  %h1=t 'student.counseling.title'

= render 'shared/flashes'

.row
  = form_for [:student,@student_counseling_session], html: { multipart: true, class: 'form-horizontal' }  do |f| 
    = render 'shared/error_messages', object: f.object
    = f.hidden_field :college_id, value: current_user.college_id
    .row
      .col-md-offset-1
        / Nav tabs
        %ul.nav.nav-tabs
          %li.active= link_to (t 'student.counseling.appointment'),"#appointment_edit", "data-toggle" => "tab"

        / display 
        .tab-content{style:"padding-top:1em"}
          #appointment_edit.tab-pane.active
            .col-md-10
              =render 'tab_appointment_edit', :f=>f

    %br
    .row
      .form-group
        .col-md-1.col-md-offset-3
          = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), student_student_counseling_sessions_path, :class => 'btn btn-default'
        .col-md-2
          .div{style: "margin-left:-20px;"}
            = f.submit t('create'), class: "btn btn-primary"
