.row{:id => "row_#{@librarytransaction.id}"}
  .col-sm-2= @librarytransaction.try(:accession).try(:accession_no)
  .col-sm-7= link_to @librarytransaction.try(:accession).try(:book).try(:title), '#', class: "bogus"
  .col-sm-2
    =@librarytransaction.returnduedate
    - @transaction_id=@librarytransaction.id
    =link_to " (return book)", library_librarytransaction_path(@transaction_id), method: :delete, remote: true
    /=@librarytransaction.returnduedate