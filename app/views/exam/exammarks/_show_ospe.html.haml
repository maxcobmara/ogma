%tr
  - if count == 0
    - if @examquestions.count>0
      %td{:rowspan=>"#{@examquestions.ospeq.count}"} OSPE
    - else
      %td{:rowspan=>"#{@qcount[7]}"} OSPE
  %td=count+1
  %td
    - if @examquestions.count > 0
      =@examquestions.ospeq[count].question.html_safe
    - else
      \-
  %td
    /- @ospe_totalmarks=0
    /- @exam_template.question_count.each{|k,v|@ospe_totalmarks=v['count'].to_i*1 if k=="ospe"}
    - @ospe_totalmarks=@qmarks_ea[7]*@qcount[7]
    - if task==1
      = number_with_precision(@marks_for_questions[count2].student_mark, precision: 2)
    - elsif task==2
      = text_field "exammark[marks_attributes][#{count2}]",:student_mark, :value=>number_with_precision(@exist_mark,:precision=>2) , :size=>2, :class=>'k1'
      /HIDDEN FIELD FOR ID OF EXISTING RECORD-edit existing record instead of creating a new one
      - if @exammark && @marks_for_questions.count!=0
        = hidden_field "exammark[marks_attributes][#{count2}]", :id, :value => @marks_for_questions[count2].id
  %td
    \/
    - if @examquestions.count>0
      =number_with_precision(@examquestions.ospeq[count].marks, precision: 2) 
    - else
      =number_with_precision(@ospe_totalmarks/@qcount[7], precision: 2)


