.page-header

- if user_signed_in?
 .row
  - if Staff.where(coemail: current_user.email).count > 0 || Student.where(semail: current_user.email).count > 0
   .alert.alert-info
    - staffexist = Staff.where(coemail: current_user.email).first
    - studentexist = Student.where(semail: current_user.email).first
    Your login email matches the following account :
    %strong #{staffexist.try(:name) || studentexist.try(:name)}
    - if studentexist.nil?
     - existing = staffexist.id
    - elsif staffexist.nil?
     - existing = studentexist.id
    - unless User.all.pluck(:userable_id).include?(existing)
     Click
     = link_to 'here', link_user_path(current_user)
     to link these accounts


.row
 .col-md-3
  .row
   .panel.panel-danger
    .panel-heading Notifications
    .panel-body
     Notifications Go Here

  .row{:style => "background-color:#D0E8F5;"}
   = render 'bulletin'

 .col-md-6
  .panel.panel-warning
   .panel-heading Day Calendar
   .panel-body{:style => "min-height:150px;"}
    Calendar Goes Here <BR>
    - @bulletins.each do |bulletin|
     = bulletin.content



 .col-md-3.centre
  .panel.panel-info
   .panel-heading Events
   .panel-body
    We'll put events stuff here
